---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/main.rs
  - src/models.rs
  - src/sources/mod.rs
  - src/util/filename.rs
  - tests/fixtures/scan_basic.xml
  - tests/fixtures/scan_multi_host.xml
  - tests/fixtures/scan_minimal_service.xml
  - tests/fixtures/scan_basic.txt
  - tests/fixtures/scan_basic.grep
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "cargo build compiles without errors"
    - "VulnSource trait is defined with name() method and Send + Sync bounds"
    - "VulnLookupError has three variants: Empty, RateLimited, NetworkFailure"
    - "ScanResult, Host, Port, Service models exist with all nmap fields as Option<T> where appropriate"
    - "sanitize_filename() wrapper exists and routes through sanitize-filename crate"
    - "Test fixture files exist for XML, text, and greppable formats"
  artifacts:
    - path: "Cargo.toml"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "quick-xml"
    - path: "src/models.rs"
      provides: "Core data types for scan results"
      contains: "pub struct Host"
    - path: "src/sources/mod.rs"
      provides: "VulnSource trait and VulnLookupError enum"
      contains: "pub trait VulnSource"
    - path: "src/util/filename.rs"
      provides: "Filename sanitization wrapper"
      contains: "pub fn sanitize_filename"
  key_links:
    - from: "src/models.rs"
      to: "src/sources/mod.rs"
      via: "models used as return types in future VulnSource methods"
      pattern: "pub struct"
---

<objective>
Initialize the PortReaper Rust project with all dependencies, define the core data models (ScanResult, Host, Port, Service), define the VulnSource plugin trait with typed error taxonomy, create the sanitize_filename wrapper, and generate test fixture files for all three nmap formats.

Purpose: Everything in Phase 1 depends on these types, this project skeleton, and these test fixtures. No parser or renderer can be built without the models they operate on.
Output: Compilable Rust project with models, trait, errors, utility, and test fixtures ready for parsers and CLI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Rust project and add all Phase 1 dependencies</name>
  <files>Cargo.toml, src/main.rs</files>
  <read_first>Cargo.toml (will not exist yet -- this is greenfield)</read_first>
  <action>
Run `cargo init --name portreaper` in the project root (since root already exists with .planning, use `cargo init` not `cargo new`).

Then add all Phase 1 dependencies:
```bash
cargo add clap --features derive
cargo add quick-xml --features serialize
cargo add serde --features derive
cargo add thiserror
cargo add is-terminal
cargo add owo-colors
cargo add anyhow
cargo add regex
cargo add sanitize-filename
```

Do NOT add serde-saphyr yet -- YAML output is not needed until Phase 3 (per research open question 1).

Replace the generated src/main.rs with a minimal stub that declares all modules:
```rust
mod cli;
mod models;
mod parser;
mod render;
mod sources;
mod util;

fn main() {
    println!("portreaper: not yet implemented");
}
```

Create empty module files so the project compiles:
- src/cli.rs (empty, just a comment: `// CLI argument definitions -- Plan 03`)
- src/parser/mod.rs (empty, just: `// Parser modules -- Plan 02`)
- src/render/mod.rs (empty, just: `// Render modules -- Plan 03`)
- src/util/mod.rs with `pub mod filename;`

Verify the project compiles with `cargo build`.
  </action>
  <verify>
    <automated>cd /home/prometheus/PortReaper && cargo build 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains `name = "portreaper"`
    - Cargo.toml [dependencies] section contains: clap, quick-xml, serde, thiserror, is-terminal, owo-colors, anyhow, regex, sanitize-filename
    - Cargo.toml does NOT contain serde-saphyr or serde_yaml
    - src/main.rs contains `mod models;` and `mod sources;` and `mod parser;` and `mod render;`
    - `cargo build` exits 0
  </acceptance_criteria>
  <done>Rust project compiles with all dependencies, module structure in place</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define core data models, VulnSource trait, error taxonomy, and sanitize_filename</name>
  <files>src/models.rs, src/sources/mod.rs, src/util/filename.rs, src/util/mod.rs</files>
  <read_first>
    - src/main.rs (verify module declarations)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: quick-xml serde structs, Pattern 4: thiserror error enum, VulnSource trait definition)
  </read_first>
  <behavior>
    - Test: ScanResult can be constructed with empty hosts vec
    - Test: Host can be constructed with all Option fields as None
    - Test: Service product/version/extrainfo are all Option&lt;String&gt; and can be None
    - Test: VulnLookupError::Empty, ::RateLimited, ::NetworkFailure are distinct variants
    - Test: VulnLookupError implements std::error::Error (Display + Debug)
    - Test: sanitize_filename("192.168.1.1:80/tcp") returns a valid filename without slashes or colons
    - Test: sanitize_filename with empty string returns non-empty fallback
  </behavior>
  <action>
**src/models.rs** -- Define normalized data models (these are the APPLICATION models, not the XML deserialization structs which live in parser/xml.rs):

```rust
/// Normalized scan result -- parser-agnostic representation.
/// All parsers (XML, text, greppable) produce this same structure.

#[derive(Debug, Clone)]
pub struct ScanResult {
    pub source: String, // filename or "stdin"
    pub hosts: Vec<Host>,
}

#[derive(Debug, Clone)]
pub struct Host {
    pub ip: String,
    pub hostnames: Vec<String>,
    pub status: String, // "up", "down"
    pub addresses: Vec<Address>,
    pub ports: Vec<Port>,
    pub os_matches: Vec<String>, // OS detection results, optional
}

#[derive(Debug, Clone)]
pub struct Address {
    pub addr: String,
    pub addr_type: String, // "ipv4", "ipv6", "mac"
}

#[derive(Debug, Clone)]
pub struct Port {
    pub port_id: u16,
    pub protocol: String, // "tcp", "udp"
    pub state: String,    // "open", "filtered", "closed"
    pub service: Option<Service>,
}

#[derive(Debug, Clone)]
pub struct Service {
    pub name: String,
    pub product: Option<String>,
    pub version: Option<String>,
    pub extra_info: Option<String>,
    pub tunnel: Option<String>,
    pub hostname: Option<String>,
    pub os_type: Option<String>,
    pub device_type: Option<String>,
    pub cpe: Vec<String>, // CPE URIs like "cpe:/a:openbsd:openssh:8.9p1"
}
```

ALL service fields except `name` MUST be `Option<T>` per locked decision and STATE.md constraint.

**src/sources/mod.rs** -- VulnSource trait + VulnLookupError:

```rust
use thiserror::Error;

#[derive(Debug, Error)]
pub enum VulnLookupError {
    #[error("no results found for {cpe}")]
    Empty { cpe: String },

    #[error("rate limited by source: retry after {retry_after_secs}s")]
    RateLimited { retry_after_secs: u64 },

    #[error("network failure querying {url}: {source}")]
    NetworkFailure {
        url: String,
        #[source]
        source: Box<dyn std::error::Error + Send + Sync>,
    },
}

/// Pluggable vulnerability data source.
/// Implementations added in Phase 2 (NVD, CVE.org).
pub trait VulnSource: Send + Sync {
    /// Human-readable name for this source (e.g., "NVD", "CVE.org")
    fn name(&self) -> &str;
}
```

The trait MUST have `Send + Sync` bounds (per STATE.md: bounded concurrency requires thread-safe sources). The `name()` method is the only required method in Phase 1. The async `lookup()` method will be added in Phase 2.

**src/util/filename.rs** -- sanitize_filename wrapper:

```rust
/// Sanitize a string for use as a filename.
/// Routes all filename construction through the sanitize-filename crate.
/// Must be called before any file-write code (per STATE.md constraint).
pub fn sanitize_filename(name: &str) -> String {
    let opts = sanitize_filename::Options {
        replacement: "_",
        ..Default::default()
    };
    let sanitized = sanitize_filename::sanitize_with_options(name, opts);
    if sanitized.is_empty() {
        "_unnamed".to_string()
    } else {
        sanitized
    }
}
```

**src/util/mod.rs**:
```rust
pub mod filename;
```

Add unit tests in each file using `#[cfg(test)] mod tests { ... }`.
  </action>
  <verify>
    <automated>cd /home/prometheus/PortReaper && cargo test -- models sources util 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/models.rs contains `pub struct ScanResult` with `pub hosts: Vec<Host>`
    - src/models.rs contains `pub struct Host` with `pub ip: String`
    - src/models.rs contains `pub struct Service` with `pub product: Option<String>` and `pub version: Option<String>` and `pub extra_info: Option<String>`
    - src/models.rs contains `pub cpe: Vec<String>`
    - src/sources/mod.rs contains `pub enum VulnLookupError` with variants `Empty`, `RateLimited`, `NetworkFailure`
    - src/sources/mod.rs contains `pub trait VulnSource: Send + Sync`
    - src/sources/mod.rs contains `fn name(&self) -> &str`
    - src/util/filename.rs contains `pub fn sanitize_filename`
    - `cargo test` exits 0 with at least 5 tests passing
  </acceptance_criteria>
  <done>All core types, trait, error enum, and filename util exist with passing unit tests</done>
</task>

<task type="auto">
  <name>Task 3: Create nmap test fixture files for XML, text, and greppable formats</name>
  <files>
    tests/fixtures/scan_basic.xml,
    tests/fixtures/scan_multi_host.xml,
    tests/fixtures/scan_minimal_service.xml,
    tests/fixtures/scan_basic.txt,
    tests/fixtures/scan_basic.grep,
    tests/fixtures/not_nmap.txt
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 for XML structure, Pattern 5 for greppable format, Pattern 6 for text format)
  </read_first>
  <action>
Create `tests/fixtures/` directory and populate with realistic nmap output files:

**tests/fixtures/scan_basic.xml** -- Single host, 3 open ports, mix of service fields present/absent:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE nmaprun>
<nmaprun scanner="nmap" args="nmap -sV -oX scan.xml 192.168.1.1" start="1710000000" version="7.94">
  <host starttime="1710000000" endtime="1710000060">
    <status state="up" reason="syn-ack" reason_ttl="64"/>
    <address addr="192.168.1.1" addrtype="ipv4"/>
    <address addr="AA:BB:CC:DD:EE:FF" addrtype="mac" vendor="Cisco"/>
    <hostnames>
      <hostname name="router.local" type="PTR"/>
    </hostnames>
    <ports>
      <port protocol="tcp" portid="22">
        <state state="open" reason="syn-ack" reason_ttl="64"/>
        <service name="ssh" product="OpenSSH" version="8.9p1" extrainfo="Ubuntu Linux; protocol 2.0" method="probed" conf="10">
          <cpe>cpe:/a:openbsd:openssh:8.9p1</cpe>
        </service>
      </port>
      <port protocol="tcp" portid="80">
        <state state="open" reason="syn-ack" reason_ttl="64"/>
        <service name="http" product="Apache httpd" version="2.4.52" method="probed" conf="10">
          <cpe>cpe:/a:apache:http_server:2.4.52</cpe>
        </service>
      </port>
      <port protocol="tcp" portid="443">
        <state state="open" reason="syn-ack" reason_ttl="64"/>
        <service name="ssl/http" method="table" conf="3"/>
      </port>
    </ports>
  </host>
</nmaprun>
```
Port 443 has NO product/version/extrainfo/cpe -- tests that optional fields work.

**tests/fixtures/scan_multi_host.xml** -- Three hosts to verify INPUT-03 (no hosts silently dropped):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE nmaprun>
<nmaprun scanner="nmap" args="nmap -sV -oX scan.xml 192.168.1.0/24" start="1710000000" version="7.94">
  <host starttime="1710000000" endtime="1710000060">
    <status state="up" reason="syn-ack" reason_ttl="64"/>
    <address addr="192.168.1.1" addrtype="ipv4"/>
    <hostnames/>
    <ports>
      <port protocol="tcp" portid="22">
        <state state="open" reason="syn-ack" reason_ttl="64"/>
        <service name="ssh" product="OpenSSH" version="7.4" method="probed" conf="10">
          <cpe>cpe:/a:openbsd:openssh:7.4</cpe>
        </service>
      </port>
    </ports>
  </host>
  <host starttime="1710000000" endtime="1710000060">
    <status state="up" reason="syn-ack" reason_ttl="64"/>
    <address addr="192.168.1.2" addrtype="ipv4"/>
    <hostnames/>
    <ports>
      <port protocol="tcp" portid="80">
        <state state="open" reason="syn-ack" reason_ttl="64"/>
        <service name="http" product="nginx" version="1.24.0" method="probed" conf="10"/>
      </port>
    </ports>
  </host>
  <host starttime="1710000000" endtime="1710000060">
    <status state="up" reason="syn-ack" reason_ttl="64"/>
    <address addr="192.168.1.3" addrtype="ipv4"/>
    <hostnames>
      <hostname name="db.internal" type="user"/>
    </hostnames>
    <ports>
      <port protocol="tcp" portid="3306">
        <state state="open" reason="syn-ack" reason_ttl="64"/>
        <service name="mysql" product="MySQL" version="8.0.32" method="probed" conf="10"/>
      </port>
      <port protocol="tcp" portid="5432">
        <state state="open" reason="syn-ack" reason_ttl="64"/>
        <service name="postgresql" method="table" conf="3"/>
      </port>
    </ports>
  </host>
</nmaprun>
```

**tests/fixtures/scan_minimal_service.xml** -- Single host where ALL optional service fields are absent (tests Pitfall 1):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE nmaprun>
<nmaprun scanner="nmap" args="nmap -oX scan.xml 10.0.0.1" start="1710000000" version="7.94">
  <host starttime="1710000000" endtime="1710000060">
    <status state="up" reason="syn-ack" reason_ttl="64"/>
    <address addr="10.0.0.1" addrtype="ipv4"/>
    <hostnames/>
    <ports>
      <port protocol="tcp" portid="8080">
        <state state="open" reason="syn-ack" reason_ttl="64"/>
        <service name="http-proxy" method="table" conf="3"/>
      </port>
    </ports>
  </host>
</nmaprun>
```

**tests/fixtures/scan_basic.txt** -- Nmap default text output:
```
Starting Nmap 7.94 ( https://nmap.org ) at 2024-03-10 10:00 UTC
Nmap scan report for 192.168.1.1
Host is up (0.0010s latency).

PORT     STATE SERVICE  VERSION
22/tcp   open  ssh      OpenSSH 8.9p1 Ubuntu 3ubuntu0.6
80/tcp   open  http     Apache httpd 2.4.52
443/tcp  open  ssl/http

Nmap done: 1 IP address (1 host up) scanned in 5.32 seconds
```

**tests/fixtures/scan_basic.grep** -- Nmap greppable output:
```
# Nmap 7.94 scan initiated Sun Mar 10 10:00:00 2024 as: nmap -sV -oG scan.grep 192.168.1.1
Host: 192.168.1.1 ()	Status: Up
Host: 192.168.1.1 ()	Ports: 22/open/tcp//ssh//OpenSSH 8.9p1/, 80/open/tcp//http//Apache httpd 2.4.52/, 443/open/tcp//ssl|http///	Ignored State: filtered (997)
# Nmap done at Sun Mar 10 10:05:32 2024 -- 1 IP address (1 host up) scanned in 332.00 seconds
```

**tests/fixtures/not_nmap.txt** -- A non-nmap file for error handling tests:
```
This is just a random text file.
It has nothing to do with nmap output.
No ports, no hosts, no services here.
```

All fixture files must be syntactically valid representations of their respective formats (XML must be well-formed, greppable must follow the tab-delimited format exactly).
  </action>
  <verify>
    <automated>cd /home/prometheus/PortReaper && test -f tests/fixtures/scan_basic.xml && test -f tests/fixtures/scan_multi_host.xml && test -f tests/fixtures/scan_minimal_service.xml && test -f tests/fixtures/scan_basic.txt && test -f tests/fixtures/scan_basic.grep && test -f tests/fixtures/not_nmap.txt && echo "All fixtures exist"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/scan_basic.xml contains `<nmaprun` and three `<port` elements and `cpe:/a:openbsd:openssh:8.9p1`
    - tests/fixtures/scan_multi_host.xml contains three `<host` elements with IPs 192.168.1.1, 192.168.1.2, 192.168.1.3
    - tests/fixtures/scan_minimal_service.xml contains `<service name="http-proxy"` with NO product or version attributes
    - tests/fixtures/scan_basic.txt contains `Nmap scan report for 192.168.1.1` and `22/tcp   open  ssh`
    - tests/fixtures/scan_basic.grep contains `# Nmap` header and `Host: 192.168.1.1` with `Ports:` field
    - tests/fixtures/not_nmap.txt does NOT contain any nmap-related keywords like `<nmaprun` or `Nmap scan report`
    - `cargo build` still exits 0
  </acceptance_criteria>
  <done>All six test fixture files exist with realistic nmap data covering single-host, multi-host, minimal-service, text, greppable, and non-nmap cases</done>
</task>

</tasks>

<verification>
- `cargo build` compiles without errors
- `cargo test` passes all unit tests for models, sources, util
- All 6 fixture files exist in tests/fixtures/
- VulnSource trait has Send + Sync bounds
- All Service fields except name are Option&lt;T&gt;
</verification>

<success_criteria>
Compilable Rust project with: core data models (ScanResult, Host, Port, Service with Option fields), VulnSource trait with typed error enum (Empty/RateLimited/NetworkFailure), sanitize_filename wrapper, and 6 test fixture files covering all nmap formats and edge cases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
